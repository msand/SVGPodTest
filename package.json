{
  "name": "SVGPodTest",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node node_modules/react-native/local-cli/cli.js start",
    "test": "jest",
    "release": "cd ios && xcodebuild -workspace SVGPodTest.xcworkspace -configuration Release -scheme SVGPodTest -sdk iphonesimulator -derivedDataPath build",
    "archive": "cd ios && xcodebuild -workspace SVGPodTest.xcworkspace -configuration Release -scheme SVGPodTest -derivedDataPath build"
  },
  "dependencies": {
    "babel-core": "^6.26.0",
    "prop-types": "^15.6.0",
    "react": "16.2.0",
    "react-native": "0.53.3",
    "react-native-svg": "^6.3.0",
    "zoomable-svg": "^3.0.1"
  },
  "devDependencies": {
    "babel-jest": "^22.1.0",
    "babel-preset-react-native": "4.0.0",
    "jest": "22.1.2",
    "react-test-renderer": "16.2.0"
  },
  "jest": {
    "preset": "react-native"
  }
}
